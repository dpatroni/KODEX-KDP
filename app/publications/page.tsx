import { AppShell } from "@/components/AppShell";import { Topbar } from "@/components/Topbar";
const pubs=[["RES-001","Spatial orientation as a magnetic design variable"],["RES-002","Architecture Explorer preliminary results"],["RES-003","Path from dipole model to FEM validation"]];
export default function Publications(){return <AppShell><Topbar title="Research Publications" subtitle="Internal white papers and technical notes prepared for collaborators."/><div className="grid gap-5 p-6 md:grid-cols-3">{pubs.map(([id,title])=><div key={id} className="panel rounded-3xl p-6"><div className="badge inline-flex rounded-full px-3 py-1 text-xs">{id}</div><h2 className="mt-5 text-2xl font-semibold">{title}</h2><p className="mt-4 text-sm leading-6 text-white/55">Draft technical note. Public summary available; full details remain private until validation and IP review.</p></div>)}</div></AppShell>}
