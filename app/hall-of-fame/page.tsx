import { AppShell } from "@/components/AppShell";import { Topbar } from "@/components/Topbar";import { candidates } from "@/lib/data";
export default function Hall(){return <AppShell><Topbar title="Hall of Fame" subtitle="Best architectures discovered so far."/><div className="grid gap-5 p-6 md:grid-cols-2 xl:grid-cols-3">{candidates.map(c=><div key={c.id} className="panel rounded-3xl p-6"><div className="badge inline-flex rounded-full px-3 py-1 text-xs">Rank {c.rank}</div><h2 className="mt-5 text-2xl font-semibold">{c.id}</h2><p className="mt-2 text-white/50">{c.family}</p><div className="mt-5 text-5xl font-semibold">{c.score.toFixed(3)}</div><p className="mt-5 text-sm leading-6 text-white/55">{c.summary}</p><button className="mt-6 rounded-full border border-white/15 px-4 py-2 text-sm text-white/75">Open candidate</button></div>)}</div></AppShell>}
