import { AppShell } from "@/components/AppShell";import { Topbar } from "@/components/Topbar";import { roadmap } from "@/lib/data";
export default function Roadmap(){return <AppShell><Topbar title="Roadmap" subtitle="From proof-of-concept to validated prototype."/><div className="p-6"><div className="panel rounded-3xl p-7"><div className="space-y-7">{roadmap.map(([name,progress]:any)=><div key={name}><div className="mb-3 flex justify-between"><span className="font-medium">{name}</span><span className="text-white/45">{progress}%</span></div><div className="h-3 rounded-full bg-white/10"><div className="h-3 rounded-full bg-gradient-to-r from-cyan-200 to-emerald-200" style={{width:`${progress}%`}}/></div></div>)}</div></div></div></AppShell>}
